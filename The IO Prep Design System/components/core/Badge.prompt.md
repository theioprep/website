Small status/grade labels (Badge) and removable filter chips (Tag).

```jsx
<Badge tone="gold">Grade 9</Badge>
<Tag onRemove={() => {}}>IB English A&L</Tag>
```

Badge tones: blue (default), gold (grades/achievements), success, error. Tag is pill-shaped with an optional × remove control.
